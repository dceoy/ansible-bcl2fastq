---
- name: Run bcl2fastq in NextSeq run directories
  hosts:
    - nextseq
  vars:
    use_docker: true
    run_user: nobody
  environment:
    https_proxy: "{{ http_proxy }}"
  roles:
    - bcl2fastq
